'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.set = set;
exports.get = get;
var storage = void 0;
var namespace = 'html5-player-';
try {
  storage = window.localStorage;
} catch (e) {
  /* ignore */
}

function set(name, val) {
  storage.setItem(namespace + name, val);
}
function get(name) {
  var val = storage.getItem(namespace + name);
  if (val === 'false' || val === 'true') {
    //处理boolean值
    val = JSON.parse(val);
  } else if (!isNaN(val) && val !== null && val !== undefined) {
    //处理数字
    val = parseInt(val, 10);
  }
  return val;
}