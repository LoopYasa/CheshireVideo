import React from 'react';

import Provider from './provider';
import HistorPlayer from './view/history';

export default function (props) {
  return React.createElement(
    Provider,
    null,
    React.createElement(HistorPlayer, props)
  );
}